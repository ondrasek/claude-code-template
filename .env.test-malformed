# Malformed .env file for testing error handling

# Valid entries
VALID_VAR=valid_value

# Invalid formats (should generate warnings)
INVALID_NO_EQUALS value without equals
=NO_KEY_VALUE
INVALID SPACES IN KEY=value
INVALID-DASHES-IN-KEY=value
123_NUMERIC_START=value

# Empty lines and comments (should be ignored)

# This is a comment
   # Indented comment

# Special characters in values
SPECIAL_CHARS=!@#$%^*(){}[]
UNICODE_CHARS=café™

# Very long line (should still work but test boundaries)
VERY_LONG_VALUE=this_is_a_very_long_value_that_goes_on_and_on_and_on_to_test_line_length_handling_in_the_env_parser_function_to_ensure_it_can_handle_extremely_long_values_without_issues

# Trailing spaces and formatting issues
TRAILING_SPACES=value with trailing spaces   
  LEADING_SPACES=value with leading spaces
MIXED_FORMATTING  =  value with mixed formatting  