{
  "mcpServers": {
    "mcp-inspector": {
      "command": "npx",
      "args": ["-y", "mcp-inspector"],
      "description": "MCP Inspector - Debug and test MCP servers interactively"
    },
    "mcp-client-cli": {
      "command": "npx", 
      "args": ["-y", "@modelcontextprotocol/client-cli"],
      "description": "MCP Client CLI - Test MCP servers from command line"
    },
    "stdio-client": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/stdio-client"],
      "description": "STDIO Client - Test stdio-based MCP servers"
    },
    "mcp-devtools": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/devtools"],
      "description": "MCP DevTools - Development tools for MCP"
    }
  },
  "development": {
    "test-server": {
      "command": "node",
      "args": ["./my-mcp-server/index.js"],
      "env": {
        "DEBUG": "mcp:*",
        "NODE_ENV": "development"
      },
      "description": "Local MCP server in development mode with debug logging"
    }
  }
}