{
  "mcpServers": {
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Persistent memory between Claude Code sessions"
    }
  },
  "_comment": "This minimal config only includes the memory server. Add other MCP servers as needed for your specific use case (databases, APIs, etc.). Claude Code has built-in filesystem and web tools."
}