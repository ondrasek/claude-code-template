{
  "mcpServers": {
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Persistent memory between Claude Code sessions"
    },
    "postgres": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://localhost/mydb"],
      "description": "PostgreSQL database connection (replace with your connection string)"
    }
  },
  "_comment": "Only included non-redundant MCP servers. Claude Code has built-in filesystem and web fetch tools."
}